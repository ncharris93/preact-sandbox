{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "sync": "npm run build && npx cap sync",
    "ios": "npm run build && npx cap run ios",
    "android": "npx cap run android",
    "preview": "vite preview",
    "docker:web:build": "./scripts/build.sh",
    "docker:web:run": "docker compose up web"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/cli": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/geolocation": "^6.0.1",
    "@capacitor/ios": "^6.1.2",
    "preact": "^10.22.1",
    "preact-iso": "^2.6.3",
    "react": "npm:@preact/compat",
    "react-dom": "npm:@preact/compat"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "eslint": "^8.57.1",
    "eslint-config-preact": "^1.5.0",
    "typescript": "^5.6.2",
    "vite": "^5.3.3"
  },
  "eslintConfig": {
    "extends": "preact"
  }
}
